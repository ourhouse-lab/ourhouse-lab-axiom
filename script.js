/* ============================================================
   AXIOM — i18n translations
   ============================================================ */
const TRANSLATIONS = {
  en: {
    'meta.title': 'Axiom — Your AI Companion',
    'meta.description': 'Axiom is an intelligent AI assistant that automates your life, learns your habits, and acts on your behalf — so you can focus on what truly matters.',
    'nav.badge': 'In Development',
    'hero.eyebrow': 'OurHouse-Lab Project',
    'hero.title.meet': 'Meet',
    'hero.tagline': 'Your intelligent companion for the age of automation.',
    'hero.description': 'Axiom is an AI-powered personal assistant that understands your context, automates your workflows, and acts on your behalf — across devices, services, and the physical world. From scheduling and research to smart home control and robotic task delegation, Axiom is the single interface between you and everything that needs to get done.',
    'hero.imgAlt': 'Axiom AI Robot',
    'pills.automation': 'Task Automation',
    'pills.ai': 'AI-Powered',
    'pills.multidevice': 'Multi-Device',
    'pills.privacy': 'Privacy First',
    'pills.robotics': 'Robotics Ready',
    'cta.label': 'Be the first to know when Axiom launches.',
    'cta.placeholder': 'your@email.com',
    'cta.button': 'Notify Me',
    'cta.sending': 'Sending...',
    'cta.success': "You're on the list! We'll reach out when Axiom is ready.",
    'features.title': 'What Axiom Will Do',
    'features.subtitle': 'A glimpse into the capabilities being built.',
    'f1.title': 'Conversational Intelligence',
    'f1.desc': 'Talk to Axiom in natural language. It understands context, remembers your preferences, and responds with precision — like a colleague who never forgets.',
    'f2.title': 'Autonomous Task Execution',
    'f2.desc': 'Delegate complex, multi-step workflows to Axiom. It plans, executes, and reports back — from booking travel to managing your calendar and inbox.',
    'f3.title': 'Smart Home & IoT Control',
    'f3.desc': 'Axiom connects to your home, your devices, and your environment. Control lighting, security, appliances, and more through a single unified intelligence layer.',
    'f4.title': 'Adaptive Personalization',
    'f4.desc': 'Axiom learns from every interaction. Over time, it anticipates your needs, proactively surfaces insights, and adapts to your evolving routines and goals.',
    'f5.title': 'Privacy by Design',
    'f5.desc': 'Your data stays yours. Axiom is built with local-first processing and end-to-end encryption, ensuring your personal information never becomes a product.',
    'f6.title': 'Robotics Integration',
    'f6.desc': 'Beyond software — Axiom is designed to interface with physical robotic systems, enabling real-world task execution through connected hardware and actuators.',
    'status.core': 'Core AI Engine',
    'status.automation': 'Automation Layer',
    'status.iot': 'IoT & Robotics',
    'status.mobile': 'Mobile App',
    'status.inprogress': 'In Progress',
    'status.planned': 'Planned',
    'status.planned2': 'Planned',
    'status.planned3': 'Planned',
    'footer.copy': '© 2025 OurHouse-Lab. Axiom is a project under active development.',
  },
  pt: {
    'meta.title': 'Axiom — Seu Companheiro de IA',
    'meta.description': 'Axiom é um assistente pessoal inteligente que automatiza sua vida, aprende seus hábitos e age em seu nome — para que você possa focar no que realmente importa.',
    'nav.badge': 'Em Desenvolvimento',
    'hero.eyebrow': 'Projeto OurHouse-Lab',
    'hero.title.meet': 'Conheça',
    'hero.tagline': 'Seu companheiro inteligente para a era da automação.',
    'hero.description': 'Axiom é um assistente pessoal movido por IA que entende seu contexto, automatiza seus fluxos de trabalho e age em seu nome — em dispositivos, serviços e no mundo físico. De agendamentos e pesquisas ao controle da casa inteligente e delegação de tarefas robóticas, o Axiom é a interface única entre você e tudo que precisa ser feito.',
    'hero.imgAlt': 'Robô IA Axiom',
    'pills.automation': 'Automação de Tarefas',
    'pills.ai': 'Movido por IA',
    'pills.multidevice': 'Multi-Dispositivo',
    'pills.privacy': 'Privacidade em Primeiro',
    'pills.robotics': 'Pronto para Robótica',
    'cta.label': 'Seja o primeiro a saber quando o Axiom for lançado.',
    'cta.placeholder': 'seu@email.com',
    'cta.button': 'Notifique-me',
    'cta.sending': 'Enviando...',
    'cta.success': 'Você está na lista! Entraremos em contato quando o Axiom estiver pronto.',
    'features.title': 'O Que o Axiom Vai Fazer',
    'features.subtitle': 'Um vislumbre das capacidades sendo desenvolvidas.',
    'f1.title': 'Inteligência Conversacional',
    'f1.desc': 'Converse com o Axiom em linguagem natural. Ele entende o contexto, lembra suas preferências e responde com precisão — como um colega que nunca esquece.',
    'f2.title': 'Execução Autônoma de Tarefas',
    'f2.desc': 'Delegue fluxos de trabalho complexos ao Axiom. Ele planeja, executa e reporta — desde reservas de viagem até o gerenciamento da sua agenda e caixa de entrada.',
    'f3.title': 'Casa Inteligente e IoT',
    'f3.desc': 'O Axiom se conecta à sua casa, seus dispositivos e seu ambiente. Controle iluminação, segurança, eletrodomésticos e muito mais por uma única camada de inteligência.',
    'f4.title': 'Personalização Adaptativa',
    'f4.desc': 'O Axiom aprende a cada interação. Com o tempo, antecipa suas necessidades, apresenta insights proativamente e se adapta às suas rotinas e objetivos.',
    'f5.title': 'Privacidade por Design',
    'f5.desc': 'Seus dados são seus. O Axiom é construído com processamento local e criptografia de ponta a ponta, garantindo que suas informações pessoais nunca se tornem um produto.',
    'f6.title': 'Integração com Robótica',
    'f6.desc': 'Além do software — o Axiom foi projetado para interagir com sistemas robóticos físicos, permitindo a execução de tarefas no mundo real por meio de hardware conectado.',
    'status.core': 'Motor de IA Principal',
    'status.automation': 'Camada de Automação',
    'status.iot': 'IoT e Robótica',
    'status.mobile': 'App Mobile',
    'status.inprogress': 'Em Andamento',
    'status.planned': 'Planejado',
    'status.planned2': 'Planejado',
    'status.planned3': 'Planejado',
    'footer.copy': '© 2025 OurHouse-Lab. Axiom é um projeto em desenvolvimento ativo.',
  },
};

let currentLang = localStorage.getItem('axiom-lang') || 'en';

function applyTranslations(lang) {
  const t = TRANSLATIONS[lang] || TRANSLATIONS.en;
  document.querySelectorAll('[data-i18n]').forEach(el => {
    const key = el.getAttribute('data-i18n');
    if (!t[key]) return;
    const attrTarget = el.getAttribute('data-i18n-attr');
    if (attrTarget) {
      el.setAttribute(attrTarget, t[key]);
    } else {
      el.textContent = t[key];
    }
  });
  document.documentElement.lang = lang;
  document.querySelectorAll('.lang-btn').forEach(btn => {
    btn.classList.toggle('active', btn.getAttribute('data-lang') === lang);
  });
  localStorage.setItem('axiom-lang', lang);
  currentLang = lang;
}

document.querySelectorAll('.lang-btn').forEach(btn => {
  btn.addEventListener('click', () => applyTranslations(btn.getAttribute('data-lang')));
});

applyTranslations(currentLang);

/* ============================================================
   AXIOM — Neural Network Canvas Animation + Form handling
   ============================================================ */

(function () {
  const canvas = document.getElementById('bg-canvas');
  if (!canvas) return;
  const ctx = canvas.getContext('2d');

  let W, H;

  // ---- Config ----
  const LAYER_COUNTS   = [5, 8, 10, 8, 5];   // neurons per layer
  const NODE_RADIUS    = 4;
  const PULSE_SPEED    = 0.012;               // how fast pulses travel (0–1 per frame)
  const PULSE_INTERVAL = 60;                  // frames between new pulses
  const ORANGE         = '#FF6B35';
  const GOLD           = '#FFD700';
  const DIM_COLOR      = 'rgba(255, 107, 53, 0.12)';
  const CONN_COLOR     = 'rgba(255, 107, 53, 0.07)';

  let nodes  = [];   // { x, y, layer, index, glowAlpha }
  let conns  = [];   // { from, to }  (indices into nodes[])
  let pulses = [];   // { conn, t, color }
  let frame  = 0;

  // ---- Build network ----
  function buildNetwork() {
    nodes = [];
    conns = [];
    pulses = [];

    const layerCount = LAYER_COUNTS.length;
    const layerSpacingX = W / (layerCount + 1);

    LAYER_COUNTS.forEach((count, li) => {
      const x = layerSpacingX * (li + 1);
      const spacingY = H / (count + 1);
      for (let ni = 0; ni < count; ni++) {
        nodes.push({
          x,
          y: spacingY * (ni + 1),
          layer: li,
          index: ni,
          glowAlpha: 0,
        });
      }
    });

    // Connect every node in layer L to every node in layer L+1
    for (let li = 0; li < LAYER_COUNTS.length - 1; li++) {
      const fromNodes = nodes.filter(n => n.layer === li);
      const toNodes   = nodes.filter(n => n.layer === li + 1);
      fromNodes.forEach(fn => {
        toNodes.forEach(tn => {
          conns.push({ from: nodes.indexOf(fn), to: nodes.indexOf(tn) });
        });
      });
    }
  }

  // ---- Spawn a pulse on a random connection ----
  function spawnPulse() {
    if (conns.length === 0) return;
    const conn  = conns[Math.floor(Math.random() * conns.length)];
    const color = Math.random() > 0.4 ? ORANGE : GOLD;
    pulses.push({ conn, t: 0, color });
  }

  // ---- Draw ----
  function draw() {
    ctx.clearRect(0, 0, W, H);

    // Draw connections
    conns.forEach(c => {
      const a = nodes[c.from];
      const b = nodes[c.to];
      ctx.beginPath();
      ctx.moveTo(a.x, a.y);
      ctx.lineTo(b.x, b.y);
      ctx.strokeStyle = CONN_COLOR;
      ctx.lineWidth = 0.6;
      ctx.stroke();
    });

    // Draw pulses
    pulses.forEach(p => {
      const a = nodes[p.conn.from];
      const b = nodes[p.conn.to];
      const px = a.x + (b.x - a.x) * p.t;
      const py = a.y + (b.y - a.y) * p.t;

      // Glow trail
      const grad = ctx.createRadialGradient(px, py, 0, px, py, 10);
      grad.addColorStop(0, p.color.replace(')', ', 0.9)').replace('rgb', 'rgba').replace('#FF6B35', 'rgba(255,107,53,0.9)').replace('#FFD700', 'rgba(255,215,0,0.9)'));
      grad.addColorStop(1, 'rgba(0,0,0,0)');

      ctx.beginPath();
      ctx.arc(px, py, 10, 0, Math.PI * 2);
      ctx.fillStyle = grad;
      ctx.fill();

      // Core dot
      ctx.beginPath();
      ctx.arc(px, py, 2.5, 0, Math.PI * 2);
      ctx.fillStyle = p.color;
      ctx.fill();

      // Light up destination node when pulse arrives
      if (p.t >= 0.95) {
        nodes[p.conn.to].glowAlpha = Math.min(1, nodes[p.conn.to].glowAlpha + 0.4);
      }
    });

    // Draw nodes
    nodes.forEach(n => {
      // Outer glow when activated
      if (n.glowAlpha > 0.01) {
        const grad = ctx.createRadialGradient(n.x, n.y, 0, n.x, n.y, 18);
        grad.addColorStop(0, `rgba(255, 107, 53, ${n.glowAlpha * 0.5})`);
        grad.addColorStop(1, 'rgba(0,0,0,0)');
        ctx.beginPath();
        ctx.arc(n.x, n.y, 18, 0, Math.PI * 2);
        ctx.fillStyle = grad;
        ctx.fill();
        n.glowAlpha *= 0.94; // fade out
      }

      // Node ring
      ctx.beginPath();
      ctx.arc(n.x, n.y, NODE_RADIUS, 0, Math.PI * 2);
      ctx.strokeStyle = `rgba(255, 107, 53, ${0.25 + n.glowAlpha * 0.6})`;
      ctx.lineWidth = 1;
      ctx.stroke();

      // Node fill
      ctx.beginPath();
      ctx.arc(n.x, n.y, NODE_RADIUS - 1, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(255, 107, 53, ${0.08 + n.glowAlpha * 0.3})`;
      ctx.fill();
    });
  }

  // ---- Animate ----
  function animate() {
    frame++;

    // Spawn new pulses
    if (frame % PULSE_INTERVAL === 0) spawnPulse();
    if (frame % (PULSE_INTERVAL * 0.6 | 0) === 0) spawnPulse(); // extra density

    // Advance pulses
    pulses = pulses.filter(p => {
      p.t += PULSE_SPEED;
      return p.t <= 1;
    });

    draw();
    requestAnimationFrame(animate);
  }

  // ---- Init ----
  function init() {
    W = canvas.width  = window.innerWidth;
    H = canvas.height = window.innerHeight;
    buildNetwork();
  }

  init();
  animate();
  window.addEventListener('resize', () => { init(); });
})();

// ---- Email form ----
(function () {
  const form    = document.getElementById('interest-form');
  const input   = document.getElementById('email-input');
  const success = document.getElementById('form-success');
  if (!form) return;

  form.addEventListener('submit', async function (e) {
    e.preventDefault();
    const email = input.value.trim();
    if (!email) return;

    const btn     = form.querySelector('.cta-btn');
    const btnText = btn.querySelector('.btn-text');
    btnText.textContent = 'Sending...';
    btn.disabled = true;

    try {
      const existing = JSON.parse(localStorage.getItem('axiom_interests') || '[]');
      if (!existing.includes(email)) {
        existing.push(email);
        localStorage.setItem('axiom_interests', JSON.stringify(existing));
      }
    } catch (_) {}

    await new Promise(r => setTimeout(r, 800));

    form.style.display = 'none';
    success.classList.add('visible');
  });
})();
